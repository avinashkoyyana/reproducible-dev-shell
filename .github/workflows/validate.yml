name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install test tools
        shell: pwsh
        run: |
          . ./scripts/Bootstrap.Common.ps1
          Initialize-PSGalleryRepository
          Install-PSResource Pester -Repository PSGallery -Scope CurrentUser -TrustRepository -Quiet
          Install-PSResource PSScriptAnalyzer -Repository PSGallery -Scope CurrentUser -TrustRepository -Quiet
      - name: Pester
        shell: pwsh
        run: Invoke-Pester ./tests -CI
      - name: PSScriptAnalyzer
        shell: pwsh
        run: |
          $results = Invoke-ScriptAnalyzer -Path . -Recurse -Severity Error
          $results | Format-Table
          if ($results) { throw 'PSScriptAnalyzer found errors.' }

  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate Bash
        run: bash -n linux/bootstrap.sh
      - name: ShellCheck
        run: shellcheck linux/bootstrap.sh
      - name: Validate JSON
        run: |
          jq empty config/oh-my-posh.omp.json
          jq empty config/windows-terminal.fragment.json
